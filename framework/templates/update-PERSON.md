---
title: "Update — <person>, <YYYY-MM-DD>"
author: "agent (neutral digest)"
date: <YYYY-MM-DD>
person: <person>
---
<!-- NEUTRAL. Capture what was DONE, not what should change — evaluation is the feedback doc's job.
     One Explore agent per person; diff window = the date of their last feedback doc. Idempotent
     (re-running replaces the same-date file). See framework/mentorship-and-collaboration.md. -->

# Update — <person>, <YYYY-MM-DD>

**Headline:** <one line: the most important thing they did this round>

## Per-piece breakdown
- **<piece>** — `<path>` (<+N lines>) — <mechanism: what it does, factually>
- **<piece>** — `<path>` — …

## Quality flags (neutral observations, not judgments)
- <TODOs left / hardcoded values / placeholders / untested paths>

## Quotes worth preserving
> "<1–3 verbatim lines from their notes/commits worth keeping>"

## Files for the mentor to spot-check  ← the most important section
- `<path>` — <why it's worth a look>
- `<path>` — <why>

## Mapping to the last feedback round
- <prior item> — **executed / partial / not addressed**
