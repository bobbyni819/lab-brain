---
title: "START HERE — <project>"
type: daily-brief
status: "Curated CURRENT STATE. Replace concise bullets in place; never append a dated worklog here
(that goes in the handoff log). First file every human and every agent opens."
updated: <YYYY-MM-DD>
---

# START HERE — <project>

> Read this first, then `_handoff-log.md`. This is the single curated snapshot of
> where the project *is right now*. If a bullet is stale, fix the bullet.

## What this project is (2–3 sentences)
<one-paragraph orientation: the question, the approach, the current phase>

## The team & lanes
<who is on this, their role, and which lane they currently own — mirror LANES.md>
- @<person> (<role>) — <current lane>
- @<person> (<role>) — <current lane>

## Current state (replace these bullets as things change)
- <where the science/build is right now>
- <what just landed, verified>
- <what's blocked or waiting>

## Start-here: the 3 files to open first
1. `<path>` — <why>
2. `<path>` — <why>
3. `<path>` — <why>

## How we work here
- Harnesses: CS = <lane>, CC = <lane>. One harness owns a repo at a time.
- Claim a lane on `LANES.md` before working; log progress in `_handoff-log.md`.
- Findings → `Sources/<topic>-<date>.md`. Every claim cites its source.
- Full convention: `collab/harness-playbook.md`.

## Next
- <the single most important next action>
